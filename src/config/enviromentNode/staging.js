import log from 'loglevel';

const config = {
    // stage config goes here
    api: {
        base: 'https://stage.cz',
    },
    devTools: false,
    logLevel: log.levels.INFO,
};

export default config;
