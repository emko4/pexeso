import log from 'loglevel';

const config = {
    // production config goes here
    api: {
        base: 'https://production.cz',
    },
    devTools: false,
    logLevel: log.levels.ERROR,
};

export default config;
