import log from 'loglevel';

const config = {
    // dev config goes here
    api: {
        base: 'https://development.cz',
    },
    devTools: true,
    logLevel: log.levels.TRACE,
};

export default config;
